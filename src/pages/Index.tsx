import Layout from "@/components/Layout";
import TenderListings from "./TenderListings";

const Index = () => {
  return (
    <Layout>
      <TenderListings />
    </Layout>
  );
};

export default Index;
